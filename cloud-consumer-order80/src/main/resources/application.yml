server:
  port: 80

spring:
  application:
    name: consumer-order
  cloud:
    nacos:
      discovery:
        server-addr: 190.160.9.100:8848
logging:
  pattern:
    #日志打印格式
    console: logging.pattern.console=%d{MM/dd HH:mm:ss.SSS} %clr(%-5level) ---  [%-15thread] %cyan(%-50logger{50}):%msg%n